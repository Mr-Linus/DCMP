language: python 
python:
  - "3.5"
  - "3.6"
services:
  - docker
before_install:
 - docker swarm init
env:
  - DJANGO_VERSION=2.0.5
install: 
  - pip install -r requirements.txt
script: 
  - python manage.py test 
